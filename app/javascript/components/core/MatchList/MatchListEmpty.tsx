import React from 'react';
import Card from '@csnow/components/ui/Card/Card';

const MatchListEmpty: React.FC = () => (
  <Card>There are no matches in this tournament</Card>
);

export default MatchListEmpty;
