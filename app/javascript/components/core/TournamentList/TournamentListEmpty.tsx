import React from 'react';
import Card from '@csnow/components/ui/Card/Card';

const TournamentListEmpty: React.FC = () => (
  <Card>There are no tournaments matching your search</Card>
);

export default TournamentListEmpty;
