import React, { Fragment } from 'react';
import Shimmer from '@csnow/components/ui/Shimmer/Shimmer';

const TournamentListLoading: React.FC = () => (
  <Fragment>
    <Shimmer />
  </Fragment>
);

export default TournamentListLoading;
