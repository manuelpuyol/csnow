import styled from 'styled-components';
import { Layout } from 'antd';

export const AppContainer = styled(Layout)`
  height: 100%;
`;

export const AppContentContainer = styled(Layout)`
  margin-left: 200px;
`;
