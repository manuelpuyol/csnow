import styled from 'styled-components';
import { Card as AntdCard } from 'antd';

const Card = styled(AntdCard)`
  box-shadow: 0 4px 8px 0 rgba(84, 84, 84, 0.16);
  margin-bottom: 24px !important;
`;

export default Card;
